<view class="grid grid--cols-{{columns}}">
  <view
    class="grid__cell"
    wx:for="{{list}}"
    wx:key="id"
    data-index="{{index}}"
    bindtap="onItemTap"
  >
    <entry-icon icon="{{item.icon}}" title="{{item.title}}" />
  </view>
</view>
