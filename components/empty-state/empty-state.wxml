<view class="empty">
  <view class="empty__icon">{{icon}}</view>
  <view class="empty__title">{{title}}</view>
  <view class="empty__desc" wx:if="{{desc}}">{{desc}}</view>
  <button class="ui-btn ui-btn--primary empty__btn" wx:if="{{btnText}}" bindtap="onAction">{{btnText}}</button>
</view>
